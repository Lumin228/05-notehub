export default interface Notes {
    content: string,
    createdAT: string,
    id: string,
    title: string,
    tag: string,
    updateAt: string,
}

export default interface InitialValuesProp {
    title: string,
    content: string,
    tag: string;
}